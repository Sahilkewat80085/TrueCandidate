# TrueCandidate Backend Configuration
# Copy this to .env and fill in values

# Server
HOST=0.0.0.0
PORT=8000
DEBUG=false

# LLM (optional — system works without it)
OPENAI_API_KEY=

# Scenario replay speed (1.0 = realtime, 5.0 = 5x faster)
SCENARIO_SPEED=5.0
